# 결정 기록

## 2026-05-22 — 내부 조판자용 Preset Block Engine

### D-024. 먼저 고객 셀프 SaaS가 아니라 내부 반자동 조판 엔진으로 간다

**결정**: 교재 트랙 1차 목표를 고객 셀프 편집기가 아니라 **성진북스 내부 조판자가 HWP/복붙 콘텐츠를 받아 IDML preset 블록으로 조립하고 PDF를 납품하는 반자동 조판 엔진**으로 둔다.

**이유**:
- 상업용 품질을 만들려면 초반에는 디자인팀/조판자가 판단해야 한다.
- 고객에게 너무 많은 선택지를 주면 한글에서 직접 꾸미는 것보다 더 어려워질 수 있다.
- 내부에서 반복 작업을 하며 안전한 블록과 수정 권한을 먼저 정리한 뒤, 나중에 고객용 UI로 공개하는 편이 현실적이다.

### D-025. preset은 스타일뿐 아니라 허용 블록도 정의한다

**결정**: IDML preset은 색/폰트/마스터만이 아니라, 사용 가능한 블록 목록도 정의한다.

현재 simply-classic v0:
- `toc` — manifest에만 있고 disabled
- `part-cover`
- `passage`
- `questions`
- `quick-answer`

**데이터 흐름**:
```
blocks[] -> chapters[] -> Typst -> PDF
```

**이유**:
- `+ 지문`, `+ 문제`, `+ PART`, `+ 빠른 정답`은 그냥 UI 버튼이 아니라 preset에 종속된 조판 부품이어야 한다.
- 다른 IDML preset을 넣으면 버튼/블록/렌더러가 달라질 수 있다.

### D-026. 엔진 테스트도 상품형 QA 샘플을 통과해야 한다

**결정**: 단순 연결용 smoke test와 별개로, `blocks[]` 엔진이 상품 품질을 유지하는지 보는 **Block Engine Product QA**를 둔다.

**QA 샘플 구분**:
- `hwp-simply-classic-debug`: HWP 변환 상품 샘플.
- `block-engine-product`: 같은 상품 샘플을 `blocks[]`로 재구성한 엔진 상품 샘플.
- `block-engine-smoke`: 버튼/JSON/PDF 연결 확인용.

**통과 기준**:
- 엔진 변경 후 `block-engine-product`가 상품 샘플 품질을 유지해야 한다.
- 2026-05-22 기준 `block-engine-product`와 `hwp-simply-classic-debug` p1~p3 픽셀 diff는 0.0000.

> 왜 그렇게 정했는지를 남긴다. 무엇을 정했는지가 아니라 **왜**.

## 2026-04-27 — 프로젝트 시작

### D-001. 1단계는 웹 없이 Typst R&D만

**결정**: 회원가입·결제·인쇄 연동·웹 UI 모두 1단계에서 제외.

**이유**:
- 핵심 가설은 "콘텐츠 JSON이 깨끗한 PDF로 컴파일되는가" 단 하나.
- 웹/DB 먼저 만들면 가설 검증이 늦어지고, 가설 실패 시 매몰비용만 커진다.
- PDF 품질이 안 나오면 그 위에 뭘 올려도 의미 없음.

**대안과 기각 사유**:
- 웹 + Typst 동시 진행 → 컴파일 성공 후 정렬·금칙 미세조정에 시간 다 쓸 가능성.
- LaTeX → 한글 처리 의존성 무겁고, 모던 워크플로 아님.
- IDML/InDesign 자동화 → 라이선스 + 파이프라인 복잡도 폭증.

### D-002. AI는 핵심 경로에 두지 않는다

**결정**: 조판 파이프라인에서 LLM API 호출 0회.

**이유**:
- 비결정성 = 같은 입력에 다른 출력 = 디버깅 지옥.
- 외부 서버에 원고 보내는 건 프라이버시 리스크.
- 비용/속도/예측가능성/프라이버시를 한 번에 해결.
- AI는 콘텐츠 작성 보조(나중에 사용자 측에서)에만 검토.

### D-003. 스타일 선택 → 콘텐츠 입력 순서

**결정**: 사용자 흐름은 (스타일 미리보기 → 선택 → 콘텐츠 입력).

**이유**:
- 빈 페이지 공포증. 사용자 동기는 "예쁜 디자인 보고 거기 내 글 넣고 싶다"에서 출발.
- 스타일이 1개면 의미 약하지만, 데이터 구조를 처음부터 이렇게 잡아야 확장 가능.

### D-004. 사용자에게 폰트/자간/여백 노출 금지

**결정**: 포맷팅은 단락·제목·강조·인용까지만.

**이유**:
- 자유도 ↑ = 품질 리스크 ↑. 비전문가가 자간 만지면 망친다.
- "디자이너 없이 단행본 만들기"가 본 서비스의 핵심 약속.
- 결과물 일관성으로 브랜드 만들기.

### D-005. format_id = `sinkukpan`

**결정**: 신국판 식별자는 `sinkukpan` (한글 → 학술적 로마자 표기 ALA-LC 기반).

**이유**:
- `b6` 같은 ISO 분류는 일본·국제 출판 분류라 한국 단행본 정체성과 어긋남. 신국판은 152×225로 B6(125×176)와 다름.
- `shinkukpan`은 일본어 '신' 발음(sh-) 영향. 한국어 표기는 'sin-'.
- `sinkukpan`은 ALA-LC/매큔라이샤워에 가까워 학술계에서 통용.
- 미래에 `kukpan`(국판 148×210), `46pan`(46판 127×188) 추가 시 동일 규칙 적용 가능.

### D-006. 콘텐츠 JSON 스키마 v1

**결정**: 다음 구조로 고정. 1단계는 인라인 마크업 없음(plain text only).

```json
{
  "schema": "sungjin-book/v1",
  "meta": { "title": "...", "author": "..." },
  "blocks": [
    { "type": "chapter", "number": 1, "title": "..." },
    { "type": "heading", "level": 2, "text": "..." },
    { "type": "paragraph", "text": "..." },
    { "type": "quote", "text": "...", "by": "..." }
  ]
}
```

**이유**:
- **블록 배열 + type 디스크리미네이터**: 미래 블록 타입(image, table, footnote) 추가 시 호환. 렌더러 중립.
- **schema 버전 필드**: 향후 v2로 마이그레이션 시 분기 가능.
- **인라인 마크업 1단계 제외**: 태평천하 본문은 plain text + 따옴표만으로 충분. 인라인은 v2에서 `runs: [{text, style}]` 도입 예정. 1단계 가설 검증에 불필요.
- **chapter는 별도 블록**: 챕터 분기·홀수페이지 시작 등 조판 로직이 챕터 단위이므로 paragraph와 평면 동거시키면 안 됨.

**기각 대안**:
- Markdown 문자열 1개로 다 표현 → 렌더러별 파싱 차이로 결정론 깨짐.
- 인라인 마크업 즉시 도입 → 1단계 가설(콘텐츠↔PDF)과 무관한 복잡도 증가.

### D-007. separator_id 1단계 기본값 = `plain`

**결정**: 1단계는 챕터 사이 별도 분리자 없이 빈 줄만. id는 `plain`.

**이유**:
- `none`은 부정형이라 미래에 'none + 빈 줄' vs 'none + 아무것도 없음' 같은 모호함.
- `plain`은 중립적 양수 의미. 미래 확장(`stars`, `dingbat`, `flourish`)과 동일 패밀리.

### D-008. 변환 = Typst 네이티브 JSON 로드 (별도 스크립트 0개)

**결정**: `template.typ`에서 `#json("../../../content/foo.json")`으로 직접 읽고 렌더. JSON→Typst 변환 스크립트 안 만든다.

**이유**:
- Typst는 `json()`/`yaml()` 빌트인 지원. 변환 단계 추가 = 의존성+버그 표면 추가.
- 1단계 핵심 가설은 "JSON이 깨끗한 PDF로 컴파일되는가"인데, 중간 변환 스크립트가 끼면 가설 경로가 흐려짐.
- Node/Bun/Python 의존성 0. `typst` 단일 바이너리로 끝.
- 미래에 다른 렌더러(InDesign 자동화 등) 필요해지면 그때 별도 스크립트 추가. JSON 자체는 이미 렌더러 중립.

**기각 대안**:
- TypeScript 변환기 → 의존성·빌드 단계 증가, 1단계엔 오버엔지니어링.
- Python 변환기 → 동일 이유 + 사용자 환경에 Python 강제.

### D-009. 서비스 정체성 — 전자책 + ISBN 무료, 인쇄만 유료 (북극성, 잠정)

**결정**: 사용자가 글을 쓰면 → 전자책 PDF + ISBN까지 **무료**로 제공. 종이책 인쇄(POD)만 유료.

**상태**: **목표(North Star)**. 1단계 검증 + 시장 반응 따라 변경 가능. 확정 아님.

**이유**:
- 진입 장벽 0 — 출판사 등록·디자이너·비용 모두 면제. "디자이너 없이 단행본 만들기" 약속을 끝까지 끌고 감.
- 한 줄 가치 명제 — "무료로 책 만들기"는 흔하지만 **ISBN까지** 무료는 경쟁자 거의 없음.
- 수익은 인쇄로 — sungjinprint(Printology)가 이미 인프라. PDF 만든 작가가 자연스럽게 POD 주문 → 수익 연결.

**메커니즘 (한국)**:
- ISBN 발급 자체는 국립중앙도서관 KOLIS-NET에서 무료.
- 출판사 등록 필요(시·도청 신고제, 거의 무료) → 우리(또는 sungjinprint)가 출판사 자격 보유.
- 납본 의무 — 전자책은 파일 등록으로 갈음.

**다음 모든 결정의 기준점**: UX·기능·과금·기술 선택 시 "이게 무료 약속을 깨지 않나?", "인쇄 전환에 도움 되나?" 두 잣대로 판단.

### D-010. UX = 분할 화면 + 프리셋 (Vellum 노하우 흡수)

**결정**: 들어가자마자 **분할 화면** — 좌(글 쓰는 칸) + 우(PDF 미리보기). 위쪽에 **판형 / 스타일 드롭다운 2개**. 가입 게이트 X.

**이유**:
- Vellum(인디 작가 표준), Notion, Figma, Canva, tldraw 모두 같은 패턴 — 가입 게이트 = 작가 90% 이탈.
- "들어가자마자 즉시 사용" — D-009 무료 약속의 UX 표현.
- 가입은 **다운로드 시점만** (이메일만 받음). 그 전엔 브라우저 localStorage 자동저장.

**카피 톤**: "진짜 무료 책 만들기, 지금 해보세요" / "디자이너 없이도, 책처럼 보입니다".

### D-011. 사용자 결정 자유도 = 판형 + 스타일 (2축)

**결정**: 사용자가 고르는 건 **판형 + 스타일** 2개. 분리자(separator)는 스타일에 흡수.

**이유**:
- Jobs/Rams "less, but better". 결정할 게 적을수록 사용자 진입 빠름.
- 폰트·자간·여백은 시스템 자동 (D-004 연장 — 자유도 = 품질 리스크).
- 코드 내부는 (format, theme, separator) 3축 유지하되, UX는 2축만 노출.

### D-012. 한국 판형 4개 (단계적 추가)

**결정**: 한국 단행본 판형 4종을 단계적으로 지원.

| 판형 | 크기 (mm) | 용도 | 추가 시점 |
|---|---|---|---|
| 신국판 | 152 × 225 | 단행본 표준 | 트랙 A M1 (지금) |
| 국판 | 148 × 210 | 에세이·학술 | 트랙 A M2 |
| 4·6판 | 127 × 188 | 문고·시집 | 트랙 A M3 |
| 크라운판 | 176 × 248 | 실용서·사진책 | 트랙 A M4 |

**이유**: 한국 출판 인기 4종. 더 많으면 사용자 부담(Jobs: 적게).

### D-013. 트랙 모델 — 단계(phase)가 아닌 트랙(track)

**결정**: 작업을 단계(phase)가 아닌 **트랙(track)**으로 본다.

| 트랙 | 성격 | 진척 |
|---|---|---|
| **A. 템플릿 라이브러리** | 계속 자라남(디자이너 작업처럼) | 신국판 클래식 1조합 |
| **B. 웹 시스템** | 점진 발전(한 번 만들면 모든 템플릿 활용) | 0% |
| **C. 운영** (ISBN/납본/POD) | 필요할 때 끼움 | 0% |

**이유**:
- 1단계 PDF는 "끝나는 작업"이 아님 — 스타일·판형·테마 계속 추가됨.
- 트랙 B(웹+미리보기) 먼저 만들면 트랙 A 새 템플릿 검증 시간 1/10.
- "1단계 완성" 표현 → "트랙 A 첫 컷"으로 교체.

### D-014. 기술 스택 (장기 안정 우선)

**결정**: 5년 후 후회 안 할 보수적 선택.

| 영역 | 선택 | 1줄 이유 |
|---|---|---|
| 사이트 뼈대 | **Next.js 15 (App Router)** | 한국 인력풀 풍부, Vercel 무료 |
| 에디터 | **Tiptap v2** | ProseMirror 10년+ 검증, 한국어 사례 풍부 |
| 미리보기 | **Vercel Function + typst CLI 서버 컴파일** | 우리 템플릿 100% 호환, 무료 tier 충당 |
| 인증·DB·스토리지 | **Supabase** | 무료 tier, 한 묶음 |
| UI | **shadcn/ui + Tailwind CSS** | 2026 표준, 자유 커스텀 |
| 호스팅 | **Vercel** | 사용자 이미 보유 계정, Next.js 친화 |
| 결제(미래) | **토스페이먼츠** | 한국 표준 |
| 폰트 | **OFL 5쌍 동봉** (노토 세리프/산스 KR, 나눔명조/고딕, Pretendard) | 재현성 + 상업 이용 자유 |

**기각 대안과 이유**:
- **BlockNote** — Tiptap 위 한 겹 더, 메인테이너 1명. 우리 UX(책 미리보기)에 노션-블록 UX 강점 약함. 우리 커스텀 블록은 결국 Tiptap 레이어.
- **Typst.ts (브라우저 WASM)** — 우리 template.typ가 0.13+ 신기능(`query`, `metadata`, `cjk-latin-spacing`) 사용 중, typst.ts 호환 보장 X. 트래픽 늘 때 R&D로 점진 이전.
- **Astro** — 컨텐츠 사이트엔 좋으나 우리는 에디터 무거운 SaaS, React 생태계가 더 풍부.
- **SvelteKit** — 코드 깔끔하지만 한국 인력 적음.
- **Lexical** — Meta 백킹이지만 한국어 사례 부족, 내부 API 변동 잦음.

**위험 대비책**:
- 미리보기 1-2초 = debounce + 로딩 표시로 흡수.
- 트래픽 1만 명 초과 시 Vercel Pro $20/월 — 그쯤 인쇄 매출이 받침.

## 2026-05-19 — 트랙 B 외부 설계 채택

### D-015. 에디터 = BlockNote, 트랙 B MVP는 v5 HTML 초안 1:1 구현

**결정**: 트랙 B 첫 컷은 외부 설계자(Manus)가 만든 `editor_v5.html` 초안을 기준으로 Next.js 14에 1:1 이식. 에디터는 **BlockNote** 사용. 위치는 `web/` 폴더.

- D-014에서 Tiptap 채택했던 결정을 BlockNote로 갱신.
- D-012의 판형 4종(신국판/국판/4·6판/크라운판) → 1단계 노출은 **신국판/46배판/문고판** 3종으로 임시 변경(v5 HTML 기준).
- D-010 "분할 화면 + 가입 게이트 X" → v5 HTML은 **온보딩 폼이 먼저** 오는 구조. 가입은 여전히 없음(localStorage만), 다만 책 메타 입력은 별도 화면으로 분리.

**이유**:
- 외부 설계자가 UI 결정의 권위. 팀원 역할로 들어감.
- BlockNote는 paragraph만 허용 + 툴바/슬래시메뉴 비활성으로 사실상 깔끔한 텍스트 에디터로 쓴다. 자유도 = 품질 리스크 원칙(D-004) 유지.
- 1단계는 PDF 파이프(Typst CLI) 빼고 HTML 미리보기로 시작. `PreviewPanel`을 독립 컴포넌트로 분리해 추후 Typst PDF 응답으로 교체 예정(컴포넌트 시그니처 = `previewContent` prop만).

**보류 / 후속 결정 필요**:
- D-014 Tiptap 기각 사유("BlockNote 메인테이너 1명") — 위험 그대로. 트랙 B 안정화 후 재평가.
- D-012 판형 정의 — 46배판(188×257) / 문고판(105×148)이 사양서에 박힌 신국판 152×225와 어울리는 라인업인지 별도 검토 필요. 지금은 UI에만 노출, Typst 템플릿은 신국판만 존재.
- 트랙 A의 Typst 파이프와 이 트랙 B의 PDF 생성 버튼이 어떻게 합쳐질지는 다음 라운드.

**기각 대안**:
- 우리 박힌 순서(① JSON 스키마 v2 → ② Typst 서버 배치 → ③ Next.js)대로 진행 — 외부 설계 권위와 충돌. 사용자가 외부 설계 우선 결정.
- v5 HTML 무시하고 우리 식으로 다시 설계 — 동일.

## 2026-05-19 — 정찰 종합: WYSIWYG 단일 엔진으로

> 4팀(Typst 본진 / typst.ts / KLREQ / 경쟁자) 정찰 결과: `docs/research/2026-05-typst-recon-summary.md`

### D-016. 미리보기 엔진을 Typst로 통일 (트랙 A·B 단일 엔진)

**결정**: 화면 미리보기를 `typst.ts` 브라우저 WASM 컴파일러로 교체. 같은 `.typ` 소스가 서버 PDF와 화면 SVG를 동시에 만든다. 기존 React+CSS 미리보기(트랙 B)는 단계적 제거.

**이유**:
- Vellum이 상업 1위를 지키는 이유는 "화면=PDF" 신뢰. Atticus는 어긋남 불만이 많아 신뢰 잃음.
- 우리 현재 구조(서버 Typst + React 미리보기)는 두 엔진 결과가 어긋남. 상업 제품으로 결격.
- typst.ts(Apache-2.0, v0.6.0 안정)가 사실상 유일한 브라우저 컴파일러. 본진 직접 빌드는 사실상 재구현 비용.

**기각 대안**:
- Paged.js 단일 엔진 → 한글 조판 품질이 Typst보다 낮음. 단어 단위 줄바꿈, 자간 조절 약함.
- React+CSS 미리보기 정밀화 → 영원히 PDF와 100% 일치 불가, 근본 결격.

### D-017. 한국어 조판은 `sungjin-book-classic` 패키지로 분리 (본진 fork 보류)

**결정**: 한국어 단행본 룰(KLREQ MVP 7룰)은 Typst 본진을 건드리지 않고 **`sungjin-book-classic` 패키지**로 구현. 입력 텍스트 전처리(금칙 사전·한영 자간 정규식)와 Typst 패키지 룰을 결합. fork는 본진이 막힐 때만 발동.

**이유**:
- Typst 본진 메인테이너 Laurenz Mädje 1인 게이트. 큰 변경은 issue 사전 합의 필수. 우리 일정에 의존하면 안 됨.
- Universe에 한국어 단행본 템플릿이 비어있음 → 패키지 publish하면 한국어 거점 선점.
- 패키지는 우리가 통제. 본진 변경에 영향받지 않음.

**예외**: `Lang::KOREAN`을 CJK 세그멘터에 라우팅하는 작은 패치는 본진 PR 시도(반려되면 우리 전처리로 흡수).

### D-018. KLREQ MVP 7룰 채택 (G1, G2, J1, I1, S1, C1, P1)

**결정**: 첫 3개월 내 구현할 한국어 조판 룰은 다음 7개로 한정.

| 코드 | 룰 | KLREQ § |
|---|---|---|
| G1 | 줄 처음 금칙(닫는괄호·구두점·줄임표 11종) | 7.1.2 |
| G2 | 줄 끝 금칙(여는괄호 9종) | 7.1.3 |
| J1 | 양끝 정렬(어절 공백 우선 분배) | 7.2.1 |
| I1 | 단락 들여쓰기(장 첫단락 0, 둘째부터 1em) | 7.2.3.1 |
| S1 | 한영 자간 1/4각 자동 | 7.3.2 |
| C1 | 장 시작 홀수 페이지, 시작 쪽번호 미표시 | — |
| P1 | 쪽번호 하단 바깥쪽 9pt 미러링 | 8.4.2 |

**이유**:
- 성범님 검증 기준("이거 책이네")을 통과시키는 최소 집합.
- 출처: KLREQ + 한국 출판 관행. 인디자인 합본자가 기본으로 해주는 것.
- 2단계 보류: G3 단어 분할 금지, S2 기호 자간, N1 각주, J1 자간 분배, 위젯/고아줄. 우선 본문이 어색하지 않은 수준까지만.

### D-019. 첫 로딩 전략 = SSR 사전 렌더 + Lazy 컴파일러 + Service Worker

**결정**: 모바일 LTE 8초 목표 달성을 위해 세 가지 전략 동시 적용.

1. **SSR 사전 렌더**: 첫 화면은 서버에서 Typst로 컴파일한 샘플 SVG를 즉시 표시. 컴파일러 다운로드 대기 없음.
2. **Lazy 컴파일러**: 사용자가 "수정 시작" 시점에 컴파일러 WASM(~8.5MB Brotli) 로드.
3. **Service Worker 캐시**: 두 번째 방문부터 WASM + 폰트 캐시 적중.

**이유**:
- 컴파일러 WASM이 27MB raw / 8.5MB Brotli. LTE 5Mbps면 순수 다운로드만 18초. 즉시 로드 시 모바일 첫 진입 망함.
- Figma 사례: 프리로드+병렬 디코딩으로 p75 300-500ms 절감, 로딩 3배 단축. 검증된 패턴.
- 폰트는 Noto Serif CJK KR 슬림 서브셋(0.31-0.48MB) + 표준 서브셋(1.44MB) 2단계.

**기각 대안**:
- 즉시 풀 다운로드 → 모바일 LTE에서 10초+, 이탈 폭발.
- 모든 미리보기 서버 렌더 → 실시간 입력 불가, Vercel function 비용 폭발.

### D-020. 포지셔닝 = "Vellum 신뢰 + Reedsy 가격 + 부크크 출구"

**결정**: 제품 한 줄 포지셔닝을 위 3요소로 고정.

- **Vellum 신뢰**: 화면=PDF. Typst 단일 컴파일로 더 엄격하게 보장(D-016).
- **Reedsy 가격**: 무료. ISBN까지 무료(D-009 연장). 인쇄만 유료.
- **부크크 출구**: 부크크/교보 POD 입고 PDF 표준 프리셋. 한 번의 클릭으로 입고 가능한 PDF 생성.

**이유**:
- Vellum은 $249 Mac only. Atticus는 PDF 어긋남. Reedsy는 자유도 0. 셋 다 한국어 무지원.
- 부크크가 4만권/2.9만 저자(2024-02)인데 한국어 자동 조판 도구가 시장에 없음. 빈 자리.
- 글로벌 도구가 절대 안 해줄 영역(부크크 입고 표준) + 모두가 약속하는 영역(WYSIWYG)을 합친다.

**핵심 차별화 5**:
1. 한국어 단행본 단일 초점 (KLREQ 기준)
2. WYSIWYG = Typst 단일 컴파일
3. 부크크 입고 PDF 프리셋 버튼
4. Vellum 3분할 UX + 자동 레이아웃 블록
5. 자유도/표지/LLM 의도적 배제 (Scribus·InDesign이 망한 지점)

**무엇을 안 할지 확정**:
- 표지 디자인 (Canva 영역)
- 표·이미지 WYSIWYG (1단계 한정)
- LLM/AI 글쓰기 보조 (D-002 연장)
- 폰트/자간/여백 사용자 노출 (D-004 연장)


### D-021. 액센트 컬러 = 쪽빛 #2a3a5a (단일색)

**결정**: 액센트 컬러를 **쪽빛 `#2a3a5a`** 한 색으로 고정. 버튼 활성·선택·로고·강조 텍스트 등 모든 컬러 자리에 이 한 색만 사용.

**이유**:
- Vellum 디자인 분석(`docs/research/2026-05-vellum-design-deep-dive.md`) 결론: 단일 액센트가 *"장식 안 했다"* → *"고급"* 인식 트릭. Vellum 자주색 #5d2e51, 우리는 한국 단행본 톤으로 변형.
- 먹색 #1f1f1f는 본문 텍스트 색과 충돌해 "액센트 없음"이 됨. 쪽빛은 약한 채도가 살짝 살아남아 *눈에 띄지 않으면서도 한국적 톤*.
- "회색 3단계 진폭"(보고서 패턴 1번) 위에 컬러 1개만 얹는 디자인 시스템의 출발점.

**적용 토큰** (`web/styles/tokens.ts` 신설):
- `--accent`: `#2a3a5a` — 단일 액센트
- `--bg-page`: `#f4f4f3` — 차가운 회색 페이지 배경
- `--bg-pane-left`: `#f7f6f4` — 좌측 패널 (따뜻한 라이트)
- `--bg-pane-center`: `#ffffff` — 중앙 에디터
- `--bg-pane-right`: `#e8e6e3` — 우측 미리보기 (전시 케이스)
- `--text-body`: `#222` — 본문, 순흑색 회피
- `--text-muted`: `#777` — 보조

**적용 우선순위**: Sprint 2 진행 중 backbone으로 박고, Sprint 3 미리보기 통합 때 본격 사용.

**기각 대안**:
- 먹색 `#1f1f1f` — 본문 색과 동급이라 액센트 효과 사라짐.
- 두 색 병행 — D-004(노출 최소)·Vellum 단일 액센트 원칙과 충돌.


## 2026-05-22 — 입력 전략

### D-022. 내부 표준은 EduBook JSON. 입력은 adapter로 흡수.

**결정**: 콘텐츠 입력은 파일 포맷이 아니라 **EduBook JSON**을 내부 표준으로 한다. 모든 입력은 Import Adapter를 거쳐 이 JSON으로 정규화된다.

**입력 등급**:
- **1차 정식** (구조 추출 안정): HWPX, DOCX — ZIP+XML 계열, 장기 본진.
- **한국 시장 필수**: HWP — 바이너리, 변환기 또는 공식 SDK로 점진 강화.
- **보조/실험적**: PDF — "종이 위 좌표 모음"이라 자동 변환 보장 X. 추출 후 사용자 확인/수정 흐름 필수.
- **직접 입력 폼**: 항상 유지. 학원선생이 키보드로 직접.

**전체 파이프라인**:
```
입력 (HWP/HWPX/DOCX/PDF/직접입력)        디자인 (IDML)
        ↓                                  ↓
   Import Adapter                    Preset Extractor
        ↓                                  ↓
   EduBook JSON                       Preset (L1+L2)
        └────────────────┬───────────────────┘
                         ↓
                  Typst Renderer
                         ↓
                        PDF
```

**이유**:
- 이미 박힌 두 부품(IDML→preset / HWP→JSON)이 정확히 이 그림의 절반씩. 새 추상화 아님, 기존 성공 구조의 제품화.
- 입력 포맷이 바뀌어도 디자인은 그대로 (디자인 ↔ 콘텐츠 분리 — D-002·D-004 연장).
- 디자인이 바뀌어도 콘텐츠는 그대로 (preset 갈아끼기로 변형).
- 입력 어댑터를 1개씩 늘려도 typst·preset 손 안 댐 → 확장 비용 ↓.

**사용자에게 보이는 입력 라벨** (기술 용어 X):
- 가장 정확함: 한글 HWPX / 워드 DOCX
- 대부분 가능: HWP
- 확인 필요: PDF

**기각 대안**:
- 입력 포맷 1개만 받기 (HWP 전용) — 학원선생이 워드/PDF 쓰는 경우 매번 변환 부담.
- 입력 포맷별 별도 파이프라인 — typst 쪽이 입력 포맷에 결합되어 확장 비용 폭증.
- PDF도 1등급 취급 — 좌표 기반 추출 한계로 자동 100% 보장 불가, 잘못된 약속.

**관련 코드**:
- `web/lib/schema/edu-book.ts` — EduBook 표준 JSON (Zod)
- `web/public/dev/edu/sample-{single,chapter}-book.json` — 샘플
- (앞으로) `web/lib/adapters/` — adapter 모음 (HWP/HWPX/DOCX/PDF 각 1개)


### D-023. 사용자는 페이지를 편집하지 않고, preset이 허용한 블록 출력 모드만 고른다.

**결정**: 지문/문제/메모/해설이 페이지 안에서 어떻게 섞이는지는 **preset 안에 고정**한다. 사용자는 글자 좌표나 박스 크기를 직접 만지지 않고, 지문 묶음 같은 큰 블록의 `layout_mode`만 선택한다.

**첫 구현 범위**:
- `layout_mode`: `default` / `question-split` / `memo` / `wide`
- `strong`, `underline`: 글자 의미 강조. 실제 표현은 preset이 결정.
- simply-classic 1차 구현: `question-split`이면 지문 뒤 문제를 다음 column/page 흐름으로 보낸다.

**허용하는 편집**:
- 블록 역할 수정: 지문, 문제, 해설, 어휘, 빠른 정답
- 블록 순서/연결 수정: 이 문제를 어느 지문에 붙일지
- 블록 출력 모드 선택: 기본, 문제 분리, 메모칸, 지문 크게
- 내용 수정: 오타, 엔터, 스페이스, 굵게, 밑줄

**금지하는 편집**:
- 글자 크기, 자간, 행간, 좌표 직접 조정
- 박스 크기 마우스 조절
- 페이지 위 도형 직접 그리기

**이유**:
- 고객은 한글/인디자인처럼 페이지를 직접 만드는 사람이 아니라, 완성 PDF를 검수하고 구조만 고치는 사람이어야 한다.
- 80~90% 자동 추출 뒤 남는 10~20%는 디자인 편집이 아니라 구조 교정으로 채운다.
- preset이 배치 규칙을 책임져야 상품 품질이 유지된다.
