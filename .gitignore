# Dependencies
node_modules/
.pnpm-store/

# Build / Output
output/*.pdf
output/*.png
dist/
build/
.next/
.astro/

# IDML 시각 diff 입력 (저작권 콘텐츠 일시 dump)
/data.json

# Env
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Typst
*.pdf.tmp

# typst.ts WASM (npm install 후 postinstall로 자동 복사, 22MB)
web/public/wasm/

# typst.ts 폰트
# 슬림 .ttf 본체는 git에 커밋(Vercel에 python+fontTools 없어 빌드 불가).
# 폰트 갱신 시: scripts/build-fonts.mjs 로컬 실행 후 결과물 커밋.
build/fonts-src/

# typst-templates 복사본 (postinstall로 자동, 단일 source of truth는 root /typst-templates)
web/public/typst-templates/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Claude local
.claude/settings.local.json

# bkit/pdca 내부 상태 (도구 자체 캐시)
docs/.bkit-memory.json
docs/.pdca-status.json

# Playwright 스크린샷·PDF (재생성 가능, 용량)
tools/playwright/shots/
tools/playwright/shots-editor/
tools/playwright/shots-vellum/
tools/playwright/shots-typst/
tools/playwright/shots-verify/
tools/playwright/shots-pdf/
tools/playwright/shots-klreq/

# 교재형 실험 — HWP 원본·결과물 보호 (저작권/민감)
hwp/
experiments/edu-import/out/
experiments/edu-import/node_modules/
experiments/edu-import/.venv/
experiments/edu-import/package-lock.json

# IDML 자동 추출 R&D — unzip 작업물 (재생 가능, 추출기 코드만 git에)
experiments/idml-recon/*/

# 원본 인디자인 작업물 (저작권 + 큰 용량) — 시스템 안쪽 자료
/*.idml
/성진_2027_공감연구소_기출의테마DNA1.0_진행중_선생님용.pdf
/성진_2027_공감연구소_심플리_고전소설.pdf

# Source Han Serif 다운로드 .otf (재다운 가능)
build/fonts/source-han/
