// presets/gonggam-rates/colors.typ
//
// 공감연구소_기출의테마DNA1.0 정답률(선생님용) 페이지 색 견본.
// 인디자인 Swatches → typst named constants.

#let accent-strong = rgb("#0088c8") // 메인 강조 청 (번호, 정답, 정답률 강조)
#let accent-soft   = rgb("#5070b8") // 보조 청 (PART/THEME 헤더용)
#let rate-muted    = luma(150)       // 오답 정답률 회색
#let ink-body      = rgb("#222222")  // 본문 텍스트
#let ink-strong    = rgb("#111111")  // 본문 강조
#let ink-muted     = luma(110)       // 보조 텍스트 (푸터, 세로 라벨)
#let line-thin     = luma(180)
